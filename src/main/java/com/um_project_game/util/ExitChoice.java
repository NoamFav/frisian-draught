package com.um_project_game.util;

public enum ExitChoice {
    NOT_EXIT,      // User chose not to exit
    EXIT_WITHOUT_SAVE, // User chose to exit without saving
    EXIT_WITH_SAVE;    // User chose to exit with saving
}